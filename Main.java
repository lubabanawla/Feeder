public class Main {
    public static void main(String[] args) {
        Feeder g = new Feeder(500);
        g.simulateOneDay(12);
        if (g.getCurrentFood() > (Feeder g)) {
            g.getCurrentFood() == 0;
        }
        else
        {
            System.out.println(g.getCurrentFood());
        }

    }

}